# Claude Code Web - Development Configuration
#
# NOTE: This .env file is ONLY for local development when running:
#   - npm start
#   - npm run dev
#
# For production/deployment, use one of these instead:
#   - CLI arguments: cc-web -p 8080 --debug
#   - Config file: cc-web --config production.json
#   - Environment variables: PORT=8080 cc-web
#
# See README.md "Configuration Reference" for details.

# Server Configuration
# ---------------------

# Server port (default: 3000)
PORT=3000

# Host to bind to (default: 0.0.0.0)
# Use 127.0.0.1 to only allow local connections
# HOST=127.0.0.1

# Working directory for Claude (defaults to current directory)
# WORKING_DIR=/path/to/project

# Authentication & Security
# -------------------------

# Disable authentication (not recommended for production)
# AUTH_DISABLED=true

# Bypass token for Playwright/automation access
# When set, this token can be used as a password to login without knowing the user's password
# Useful for automated testing and Playwright browser automation
# Can be set via CLI: --bypass-token <token>
# DEBUG_TOKEN=your-secret-bypass-token

# Enable secure cookie flag (requires HTTPS - set to true if behind nginx/caddy with SSL)
# SECURE_COOKIE=true

# Session duration in days (default: 3)
SESSION_DURATION_DAYS=3

# Logging & Debug
# ---------------

# Enable debug mode - logs all output with timestamps
# When running as daemon, logs go to cc-web.log
DEBUG=true

# Custom log file path (optional, for daemon mode)
# LOG_FILE=/var/log/cc-web.log

# Daemon Management
# -----------------

# Custom PID file path (optional, for daemon mode)
# Default: ~/.cc-web/cc-web.pid
# PID_FILE=/var/run/cc-web.pid

# Simulate update for testing (before publishing to npm)
# SIMULATE_UPDATE=1.1.0

# Upgrade retry configuration (advanced)
# Maximum retries when binding port during upgrade/restart (default: 20)
# UPGRADE_MAX_RETRIES=20
# Interval between retries in milliseconds (default: 1000)
# UPGRADE_RETRY_INTERVAL=1000
